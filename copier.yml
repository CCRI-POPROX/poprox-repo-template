_subdirectory: template

# questions
project_name:
  type: str
  help: What is your project name (for the repo & Python distribution)?

project_descr:
  type: str
  help: What is your project description?

package_name:
  type: str
  help: What is your Python package name (importable package name)?
  default: "{{project_name | lower() | replace('-', '_')}}"